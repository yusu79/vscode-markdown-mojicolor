# AGENTS

## 基本方針

* ユーザーとのやり取りは日本語で行う
* 既存のプロジェクト構成とコーディングスタイルに従う
* 実装前に関連コードと既存仕様を確認し、影響範囲を把握する
* 依頼を達成するために必要な最小限の変更にとどめる
* 依頼と関係のないファイルやコードは変更しない
* 必要がない限り、新しい依存パッケージを追加しない
* ソースコードや既存資料から確認できる事項を推測で判断しない
* ユーザーから見た動作、公開API、互換性などに影響する判断が必要な場合は、勝手に決定せず実装前に方針を提示する
* 内部実装上の軽微な判断は、既存コードや既存の設計方針に従って決定してよい
* 作業中に依頼と無関係な問題を発見した場合は、勝手に修正せず報告する
* 実装後は `package.json` に定義されているテストと lint を実行し、結果を確認する。


## 参照するルール

開発手順、ブランチ運用、テスト、Lint、コミット、公開については `CONTRIBUTING.md` に従う。

Codexの作業手順については `docs/agent-workflow.md` に従う。

## リリース時の確認

releaseブランチでリリース作業を行う場合は、公開前に `package.json` の拡張機能識別情報を過去のリリースと比較する。

以下を必ず確認する。

1. `release-1.0.0` 時点の `displayName`

   * Marketplaceなどでユーザーに表示される拡張機能名として確認する
   * 意図しない変更が行われていないことを確認する

2. `release-1.0.0` 時点の `name`

   * 正しい拡張機能ID用の名前になっていることを確認する
   * このプロジェクトでは `vscode-*` の形式になっているか確認する

3. 現在の `release-*` と以前のリリースの `name`

   * 過去の公開済みバージョンと一致していることを確認する
   * 意図せず変更されている場合は公開を行わず、ユーザーへ報告する
   * `name` が異なると、Marketplaceなどで既存拡張機能の更新ではなく別の拡張機能として登録される可能性があるため、特に注意する

`displayName` または `name` の変更が必要な場合は、通常のバージョン更新として扱わず、公開への影響を確認してからユーザーへ方針を提示する。

### 初回リリース時の確認

`release-1.0.0` ブランチを削除する前に、GitHub ActionsからMarketplaceへ公開するために必要なPATがGitHubへ登録されていることを確認する。

* GitHub ActionsのworkflowでMarketplace公開に使用しているSecret名を確認する
* 対応するPATがGitHub ActionsのSecretsに登録されていることを確認する
* PATそのものの値を表示・記録しない
* PATが未登録、または登録状況を確認できない場合は、`release-1.0.0` ブランチを削除せずユーザーへ報告する

初回リリースでは、少なくとも以下の状態を確認してからreleaseブランチの削除へ進む。

```text
package.json
├─ displayName が正しい
└─ name が vscode-* の正しい値

GitHub Actions
├─ タグpushによる公開workflowが存在する
└─ Marketplace公開用PATのSecretが登録されている

Marketplace
└─ 意図した拡張機能として公開できる状態になっている
```

## プロジェクト概要

VS CodeのMarkdownプレビューで文字色を指定できる拡張機能「Markdown MojiColor」を開発するプロジェクト。

`%文字%{色}` という独自記法を使用し、Markdownプレビュー上で指定した文字色としてレンダリングする。

文字色の変換処理にはnpmパッケージ `markdown-it-mojicolor` を使用し、このリポジトリではVS CodeのMarkdown拡張ポイントを通してmarkdown-itへプラグインを組み込む。

色指定はCSSの色名、16進数、RGB、HSLのほか、日本語の色名や和色名などに対応する。

文字色変換の中核ロジックは `markdown-it-mojicolor` 側に置き、このプロジェクトでは主にVS Codeとの連携、拡張機能としての設定、テスト、パッケージング、公開を担当する。

